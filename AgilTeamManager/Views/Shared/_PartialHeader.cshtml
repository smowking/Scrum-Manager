<div class="Geral">
    <div class="Titulo">@ViewBag.Title</div>
    @if (ViewBag.Description != null)
    {
        <div class="Descricao">@ViewBag.Description</div>
    }
</div>

<style>
    .Geral {
        margin-left: 210px;
        height: 40px;
        margin-top: 60px;
        background-color: #ecf0f5;
        line-height:40px;
    }

    .Titulo {
        display: inline;
        font-size: 25px;
    }

    .Descricao {
        opacity:0.7;
        margin-left: 10px;
        display: inline;
        font-size: 12px;
    }

</style>